// IAuthService.ts
/*
import { RegisterData, User } from '../models/User';

export interface IAuthService {
    // This method handles the entire registration business flow
    registerUser(userData: RegisterData): Promise<User>; 
}
    */